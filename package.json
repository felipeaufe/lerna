{
  "name": "root",
  "private": true,
  "scripts": {
    "version": "lerna version",
    "change": "lerna changed",
    "publish": "lerna publish --conventional-commits --yes",
    "prepublish": "git add . && git commit -m 'prepublish commit' || true",
    "build": "lerna run build"
  },
  "publishConfig": {
    "@felipeaufe:registry": "https://npm.pkg.github.com"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
