{
  "name": "root",
  "private": true,
  "scripts": {
    "version": "lerna version --loglevel verbose",
    "change": "lerna changed",
    "publish": "lerna publish",
    "build": "lerna run build"
  },
  "publishConfig": {
    "@felipeaufe:registry": "https://npm.pkg.github.com"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.1.4"
  }
}
