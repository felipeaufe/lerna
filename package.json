{
  "name": "@felipeaufe/lerna",
  "private": true,
  "scripts": {
    "change": "lerna changed",
    "prepublish": "npm run version",
    "publish": "lerna publish from-git --conventional-commits --yes dist",
    "build": "lerna run build"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
